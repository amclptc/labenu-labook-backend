export interface signupInputDTO {
    name: unknown,
    email: unknown,
    password: unknown
};

export interface signuptOutputDTO {
    token: string
};

export interface loginInputDTO {
    email: unknown,
    password: unknown
};

export interface loginOutputDTO {
    token: string
};

